---
name: bid-doc-html
description: >-
  This skill generates bid-ready product brochures / white papers as a single self-contained
  HTML page. Use it when the user wants to create 投标产品说明书 / 产品白皮书 / 投标画册 from
  投标参数 and paired images, where each paragraph is accompanied by its corresponding
  image(s). Trigger phrases include 投标说明书, 产品白皮书, 投标画册, 产品画册, 参数配图文档,
  "根据参数做一份图文说明书". Default style is a furniture-catalog look (large product images,
  image-text side-by-side, generous whitespace) and Chinese copy.
agent_created: true
---

# Bid Doc HTML（投标图文说明书 / 白皮书生成器）

## Overview

把用户提供的「投标参数 + 对应图片」快速转换成一份排版精良、图文一一对应的网页版产品说明书 / 白皮书。
默认输出单页自包含 HTML（图文并排、留白干净、画册感），默认中文文案。

## Locked Default Preferences（一次性锁定，除非用户本次特别说明，否则沿用）

- **输出格式**：**单文件 HTML，图片以 base64 内嵌**（含内联 CSS，零外部依赖，可直接邮件/微信分享不丢图）。需要轻量可编辑版本时再单独导出 `images/` 文件夹版。
- **视觉基调**：家具介绍风格 / 画册风——大图为主、图文并排或上下叠放、留白充足、配色克制（浅底 + 一个强调色）。参考 `assets/template.html` 的版式与 CSS。
- **语言**：简体中文（用户要求中英双语时再切换）。
- **阅读方式**：桌面与手机均自适应；打印时 A4 友好。

## Standard Brief Template（让用户每次照填，复制即用）

要求用户按以下结构提交材料，避免自由长文描述：

```
【任务类型】投标产品说明书 / 白皮书
【投标项目】xxx项目；评委关注点：性能 / 合规 / 性价比
【整体风格】沿用默认偏好（或本次特别说明，如换强调色、加封面）
【素材文件夹】<绝对路径，图片都在里面>

【参数 ↔ 配图对应表】（核心，必填）
| 段落主题   | 关键参数/卖点        | 配图编号 | 配图要说清什么     |
|------------|----------------------|----------|--------------------|
| 产品概述   | 型号、适用场景       | 图1      | 整体外观/场景      |
| 技术参数   | 处理量≥1000t/h 等    | 图2,图3  | 参数表/原理图      |

【逐段内容要求】
1. 段落1（配图：图1）：围绕xxx写3-4句，突出…
2. 段落2（配图：图2,图3）：把参数表讲清楚，强调达标…
（用户不会写文案时写"AI按参数发挥"，由本技能起草后用户改）
```

> 完整的可复制模板见 `references/brief-template.md`。

## Workflow

1. **接收与读取**：拿到素材文件夹路径和「参数↔配图对应表」。用 Read 工具读取该表与所有编号图片，确认图片编号与段落的绑定关系，记录每张图的说明文字（图注）。
2. **结构推导**：以对应表的「段落主题」为章节顺序，确定文档大纲（封面 → 各章节 → 封底/联系信息）。若用户给了逐段内容要求则直接采用，否则基于参数起草中文文案。
3. **生成 HTML**：直接运行技能自带脚本（仅依赖 Python 标准库，无需安装任何包）：
   ```
   python <skill>/scripts/make_doc.py <投标参数.docx> [输出目录] [--title "自定义标题"]
   ```
   脚本自动完成：解包 docx 提取图文 → 按"数字编号"识别章节、子项(如 11.1)折叠进父章节 → 以 `assets/template.html` 为底座注入内容 → **图片 base64 内嵌**为单文件 `index.html`。输出目录默认 `<docx同目录>/bid-doc`。
   - 每个章节：标题 + 1~N 段文案 + 对应图片（`<img>` + 图注）。图文交替左右排版营造画册感。
   - **图片默认 base64 内嵌**为单文件，可直接分享。若用户要轻量版，则改为相对路径 `images/图1.jpg` 并随附 `images/` 文件夹。
   - **图片显示用 `object-fit:contain`**，完整呈现不裁切（技术细节图尤为关键）。
   - **正文字号偏小（13.5px）、行距收紧**，与图片配比更协调。
   - **强调色高对比**（默认青绿 `#0f766e` + 近黑正文），保持简洁。
   - 若源 docx 内存在损坏/缺失的图片链接导致某章节无图，手动把对应图片放入输出 `images/` 并修改 `index.html` 的该章节 `<img src>`，或下次提供更完整的源文件。
4. **自检**：确认每段都有配图、每张图都出现在正确段落、参数数字与用户给的一致、无占位待办。
5. **交付**：用 present_files 打开预览并交付 `index.html`；提示用户如需 Word/PDF 可后续转换。

## Image Delivery Options（用户给图的三种方式）

1. 放文件夹 + 编号命名（`图1-外观.jpg`），最省事，直接读。
2. 对话里直接上传图片，用户说明"第N张对应第X段"。
3. 图未出、只有占位需求 → 文案写"此处需图：原理示意图"，先留白版式，用户后补。

## One-Shot Quality Tips（写进交付说明，帮助用户一次成稿）

- 先给一页"范例"对齐风格：用户给一份满意的旧文档/截图，本技能照其版式复刻，比纯文字描述准。
- 参数用表格给，别揉进句子：表格 → 生成"参数卡/参数表"组件；长句 → 易漏参数。
- 一次给全，不分批：图、参数、风格一次到位，减少来回。

## Annotation Feature（页面标注模式）

生成的 HTML 自带标注引擎（右下角工具条），满足"用图形把文字指向图片"的需求：

- **标注模式** 开关：开启后标注专属工具（`.annotools`）自动展开，关闭后收起保持工具栏简洁。
- **标注工具子菜单**：箭头↗、圆圈◯、线型粗细（1.5/2.4/3.5）、实线/虚线切换、撤销、清空，均在标注模式下才可见 — 由左右竖线分隔的 `.annotools` 包裹，通过 `body.annot-on .annotools{display:flex}` 控制显隐。
- **当前工具高亮**：箭头/圆圈按钮带蓝色内框高亮（`data-tool` + `markActiveTool()`），一眼看懂当前绘制工具。
- **箭头 ↗ / 圆圈 ◯**：从文字处拖到图片要点处画出图形。**无自动编号**。
- **说明文字可选**：绘制后弹窗填说明文字；**留空则只显示图形本身，不显示任何编号/标签**；**有文字时显示该文字，且在标注模式下可拖动文字调整其位置**（坐标存于 `lx/ly`）。
- **撤销 / 清空** 管理标注；标注自动存本地（localStorage）。
- **导出含标注**：把当前标注序列化并重新下载一个 HTML，标注随文件一起分享，他人打开即可见。
- 坐标按百分比存储，缩放/手机端不偏移。

## Layout Adjustment（图片与文字框均可调）

生成的 HTML 自带「调整」模式（工具条按钮，与标注/编辑模式互斥）：

- 开启后，**所有页面**的图片与文字框右下角均出现缩放手柄（青色方块），光标变 `move`。包括封面标题/副标题区、产品配置清单区、各章节正文区 — 全文覆盖。
- **图片**：拖动图片本身 → 平移；拖右下角手柄 → 缩放宽度（高度按比例自适应）。**手柄随图片移动**：位移/缩放作用于 `.imgwrap`（图片+手柄的包裹层），`.rs` 是其子元素。
- **文字框**：每个文字栏（标题+参数）也是可拖拽/缩放的文本框，拖主体平移、拖右下角手柄缩放（宽高均可调，高度超内容时内部滚动）。
- 调整结果自动存入 localStorage，「导出含标注」一并写入文件，分享后他人打开即见最终版式。
- 状态存于 `STATE.imgs[iid]={tx,ty,w}` 与 `STATE.texts[iid]={tx,ty,w,h}`。

## Themes（主题风格，可一键切换）

工具条右侧色块按钮直接切换整体配色，无需改代码（共 11 个可选，含默认 + 10 套预设）：

- **青绿（默认）** `theme-""`：沉稳青绿 + 近黑正文。
- **暖木家具** `theme-wood`：暖棕木色，贴合家具/家居画册。
- **科技蓝** `theme-tech`：蓝调，适合软硬件/科技产品。
- **简约黑白** `theme-mono`：纯黑白，极简。
- **政务红** `theme-gov`：红调，适合政府/国企项目。
- **薄荷绿** `theme-mint`：清新薄荷绿，明亮轻快。
- **活力橙** `theme-orange`：明橙，强调年轻活力。
- **暗夜深蓝** `theme-dark`：深色背景 + 浅字，夜间/高端感。
- **莫兰迪** `theme-morandi`：低饱和灰调，温柔高级。
- **雅紫** `theme-purple`：雅致紫调，文创/教育适用。
- **大地卡其** `theme-khaki`：卡其大地色，自然质朴。

实现：CSS 变量（`--accent/--bg/--ink/--muted/--accent-soft` 等 7 个变量）按 `body.theme-*` 覆盖；选择持久化于 `STATE.theme`，随「导出含标注」打包。

**深色主题自适应**：暗夜深蓝（`theme-dark`）等深色背景主题自动适配：
- Body 背景色独立覆盖（`#0a0f1e`），与 A4 纸面 `--bg` 区分。
- 所有硬编码深色文字（如 `.text p`、`.intro-sec ul`）使用 `var(--muted)` / `var(--ink)`，不再直接写死色值，确保深色背景上文字浅色可读。
- 标注标签 `.alabel` 描边改为 `var(--bg)` 而非白色，融入暗色背景。
- 工具栏/格式条按钮背景改为 `var(--accent-soft)`，按钮文字色用 `var(--ink)`。
- 每个主题独立定义 `--muted`（次要文字）和 `--img-bg`（图片占位背景），不再沿用亮色默认值。

## Text Editing（文字编辑：选区级内联格式化）

工具条「编辑」模式：开启后**所有文字块（`.text`）自动变为 `contenteditable`**，包括封面标题/副标题、产品配置清单、各章节正文。**选中任意文字片段**后，顶部 `fmt-bar` 提供以下选区级格式化：

- **直接编辑**：编辑模式下所有文字区域可自由输入/删除/修改文字内容，光标变为 I-beam，聚焦时有醒目的强调色 outline 视觉效果。
- **选中字体** `#fmt-font`：对选中文字应用字体（宋体/黑体/楷体/微软雅黑/仿宋/Arial），使用 `execFontStyle` 包裹 `<span style="font-family:...">`。
- **字号** `#fmt-size`：对选中文字应用字号（12/14/16/18/20/24/32px），包裹 `<span style="font-size:...">`。
- **粗细** `#fmt-weight`：对选中文字应用字重（300/400/500/600/700/800/900），包裹 `<span style="font-weight:...">`。与加粗独立（可同时设细体+加粗标签）。
- **加粗 B** `#fmt-bold`：对选中文字 toggle 加粗（`document.execCommand('bold')`），浏览器原生支持 `<b>`/`<strong>` 标签的嵌套切换。
- **下划线 U** `#fmt-underline`：对选中文字 toggle 下划线（`document.execCommand('underline')`），浏览器原生切换。
- **文字颜色**：8 个预设色块（黑/红/青绿/蓝/橙/紫/绿/浅灰）+ 一个取色器 `<input type="color">`，对选中文字应用 `color`。
- **全文字体** `#fmt-global-font`：独立于选区级的全文默认字体切换（见「Global Font」节），通过 CSS 变量 `--font` 控制。

所有内联样式（span）由浏览器原生管理，`Ctrl+Z` 可撤销。「重置」按钮通过 `stripFormatting()` 剥离所有 `<span>/<b>/<strong>/<u>/<font>` 标签还原原始文字。编辑模式与标注/调整模式互斥。

## Global Font（全局字体切换）

编辑模式下 fmt-bar 的「全文字体 ▾」下拉实现全文一键切换默认字体，无需逐块选中：

- **默认（不选）**：使用系统默认字体栈（PingFang SC / Microsoft YaHei / Noto Sans SC）。
- **可选字体**：宋体（SimSun）、黑体（SimHei）、楷体（KaiTi）、微软雅黑、仿宋（FangSong）、Arial。
- **实现**：CSS 变量 `--font` 挂载到 `body`，通过 `body.style.setProperty('--font', ...)` 动态切换。所有 `.text` 块自动继承，无需遍历 DOM。
- **持久化**：存于 `STATE.font`，随 localStorage 和「导出含标注」一并保存/分享。
- **重置**：重置按钮同时清空全局字体设置，恢复默认。

## Annotation Line Type & Thickness（标注线型与粗细）

标注模式下，`.annotools` 子菜单内提供线型 / 粗细控件（仅在标注模式开启时可见）：

- **粗细** `data-act="lw"`：三档可选 `1.5 / 2.4（默认）/ 3.5` px，作用于箭头线宽与圆圈描边。
- **线型** `data-act="dash"`：开关按钮，开启后箭头/圆圈使用虚线（`stroke-dasharray`）。

存于 `STATE.lineW` 与 `STATE.dash`，随导出打包。绘制前选择，对所有新绘图形生效；已绘制图形沿用其绘制时的设置。

## Caption Editing（图名编辑与显示开关）

每个图片的图注（`<figcaption>`）可直接在页面编辑，并有整体开关：

- **图名编辑**：标注/编辑模式下图注可 `contenteditable` 直接改字，内容存于 `STATE.captions[iid]`，随导出打包。
- **显示开关** `data-act="cap"`：工具条按钮切换是否展示图名（`STATE.showCaptions`，默认开）。关闭后所有图注隐藏，便于纯图展示。

图注与图片绑定在同一个 `.imgwrap` 内，调整图片位置时图注跟随移动。

## A4 / PDF Ready（页面尺寸）

- 内容包裹在 `.page` 容器：`width:210mm; min-height:297mm`（标准 A4），居中显示为白纸，便于后续导出 PDF 即为一页页 A4。
- 打印样式：`@page{size:A4;margin:0}`，各章节 `page-break-inside:avoid`，封面/章节不跨页断裂。
- 导出 PDF 建议：浏览器「打印 → 另存为 PDF」，纸张选 A4、边距 0，即可得到与屏幕一致的 A4 文档。

## Reset（重置功能）

工具条红色「重置」按钮，带 confirm 确认提示，一键清空所有手动操作：
- 删除所有标注（`STATE.shapes = []`）
- 还原所有图片/文字框位置与大小（`STATE.imgs = {}`, `STATE.texts = {}`）
- 剥离所有内联格式化标签（`stripFormatting()` 移除 `<span>/<b>/<strong>/<u>/<font>`，还原纯文本）
- 恢复默认线型粗细（2.4px）和实线
- 恢复图名编辑内容、重置显示开关为「开」
- 恢复全文默认字体（`STATE.font = ""`，清除 `--font` 变量）
- DOM 同步还原所有内联样式

## Editor Application（全功能文档编辑器）

生成 `index.html` 后，可在同目录打开 `editor.html` 进入**全功能文档编辑器**，对内容进行任意修改：

- **所有文字直接编辑**：点击任意文字即可修改（标题、副标题、清单项、章节正文），无需切换模式。
- **自由添加章节**：顶部工具栏「+ 添加章节」在末尾插入新章节；章节之间悬停显示「+ 在此添加章节」按钮在任意位置插入。
- **自由添加图片**：「+ 添加图片」向最后一节追加图片；每节图片区悬停显示「替换」/「删除」按钮；图片区无图时显示虚线框点击上传。
- **章节管理**：鼠标悬停章节右上角显示操作按钮：上移、下移、删除。
- **11 套主题一键切换**：顶部色块按钮即时更换全文档配色。
- **全文字体切换**：下拉选择宋体/黑体/楷体/雅黑/仿宋/Arial。
- **导出独立 HTML**：「📥 导出 HTML」生成自包含的单文件（含所有图片 base64），可直接分享。
- **自动保存**：所有修改自动存入浏览器 localStorage，刷新不丢失。
- **底部模式栏**：标注模式（箭头/圆圈/线型/撤销）+ 调整模式（图片/文字框拖拽缩放）+ 格式编辑模式（选中文字改字体/字号/粗细/颜色），与原模板功能完全兼容。

## Resources

### scripts/
- `make_doc.py`：端到端生成器（仅依赖 Python 标准库，零安装）。输入投标参数 `.docx`，输出单文件 `index.html`（base64 内嵌图、含标注引擎（子菜单+工具高亮+线型控制）、图片/文字框调整（含封面+配置清单）、选区级内联文字格式化（字体/字号/粗细/颜色）、全文字体切换、图名编辑/开关、重置、11 套深色自适应主题）。
  - 用法：`python make_doc.py <docx> [输出目录] [--title "标题"]`

### assets/
- `template.html`：家具画册风 HTML 底座（内联 CSS、章节交替排版、contain 完整显图、7 变量多主题配色含深色自适应、内置标注引擎 + 全文图片/文字框调整 + 全文字体切换 + 选区级内联格式化 + 图名编辑/开关 + 重置、响应式/打印友好）。由 `make_doc.py` 注入内容 + base64 图片。
- `editor.html`：**全功能文档编辑器应用**。从 docx 生成后可在编辑器中自由修改所有文字、增删章节、上传替换图片、切换主题/字体，最终导出独立 HTML。所有文字直接 contentEditable 编辑，图片悬停显示替换/删除按钮，章节可移动/删除。底部模式栏保留标注/调整/格式编辑功能。

### references/
- `brief-template.md`：上述标准简报模板的可复制文本，交付时也可发给用户复用。
